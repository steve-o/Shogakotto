Command line tool to test analytics domain.

```
usage: Shogakotto
 -h,--help                 print this message
    --session <uri>        TREP-RT session declaration
    --symbol-path <file>   read from symbol path
    --version              print version information and exit
```

